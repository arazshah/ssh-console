services:
  ssh-console:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - APP_PASSWORD=${APP_PASSWORD}
      - APP_SECRET=${APP_SECRET}
      - PORT=3000
